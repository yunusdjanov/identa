'use client';

import { AppointmentsWorkspace } from '@/app/(protected)/appointments/appointments-workspace';

export default function DashboardPage() {
    return <AppointmentsWorkspace mode="dashboard" />;
}
